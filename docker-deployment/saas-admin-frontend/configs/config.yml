# Configuration for saas-admin-frontend (Frontend)
# Precedence: config.yml < .env < environment variables

environment: development

server:
  port: 3001
  host: "0.0.0.0"

api:
  url: http://localhost:8098
  timeout: 30s

logging:
  level: info

features:
  analytics_enabled: true
  debug_mode: false
